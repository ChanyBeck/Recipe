<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MAUI_Recipe_App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MAUI_Recipe_App"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:Home}"
        Route="Home" />

        <ShellContent
        Title="Recipe List"
        ContentTemplate="{DataTemplate local:List}"
        Route="List" />

        <ShellContent
        Title="Recipe Search"
        ContentTemplate="{DataTemplate local:RecipeSearch}"
        Route="RecipeSearch" />
    </TabBar>
    
    <ShellContent
        Title="login"
        ContentTemplate="{DataTemplate local:login}"
        Route="login" />
</Shell>
